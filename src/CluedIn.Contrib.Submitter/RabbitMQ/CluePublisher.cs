using CluedIn.Core.Data;

namespace CluedIn.Contrib.Submitter.RabbitMQ;

public static class CluePublisher
{
    public static void Publish(this Clue clue, Context context)
    {
        throw new NotImplementedException(nameof(Publish));
    }
}
